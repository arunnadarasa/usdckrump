# LayerZero Executor Worker for USDCKrumpOFT
FROM node:20-slim

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci --legacy-peer-deps

COPY . .

CMD ["node", "scripts/lz-executor-worker.js"]
